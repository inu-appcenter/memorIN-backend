이 디렉터리는 컨테이너의 /run/secrets 로 읽기 전용 마운트된다.

여기에 두는 것:
  firebase-service-account.json   (FCM 사용 시. Firebase 콘솔 > 서비스 계정 > 새 비공개 키 생성)

.gitignore가 이 디렉터리의 내용물을 전부 막는다. 이 파일(.gitkeep)만 예외다.
디렉터리 자체가 저장소에 있어야 하는 이유는, 없으면 docker compose가 마운트 시점에
빈 디렉터리를 만들면서 소유권이 root로 잡히기 때문이다.
