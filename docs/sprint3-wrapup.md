# Sprint 3 결산 — 백엔드

> 작성: 2026-08-20 (Sprint 3 W8 목 · 리뷰 회의 입력용)
> 최종 갱신: 2026-08-20 — #163 해결(#174) 반영
> 범위: **백엔드만**. FE·디자인 항목은 이 문서에서 다루지 않는다.
> 기준: `origin/develop` @ `3bc2575` + 미머지 PR [#169](https://github.com/inu-appcenter/MermorIN/pull/169)

Sprint 3에서 무엇이 실제로 끝났고 무엇이 남았는지를 **코드로 확인한** 기록이다.
Sprint 2 결산(`docs/sprint2-wrapup.md`)과 같은 방식으로, 문서의 체크박스가 아니라 실행 경로를 근거로 판정했다.

---

## 1. 게이트 대비 실제 상태

| 게이트 | 판정 | 근거 |
|---|---|---|
| 친구 피드에 팔로잉 게시물만 노출 | ✅ **충족** | `PostFeedIntegrationTest.친구피드는_팔로우한사람의_게시글만_조회한다` 외 4건, `PostVisibilityFilterTest` 6건 |
| 게시물 공유 API 통합 테스트 | ❌ **미충족** | PR #169 미머지. 테스트 파일 0개 (§3) |
| 알림 히스토리 조회 | 🟡 **API만 충족** | 조회·읽음 3개 엔드포인트 머지(#168). 그러나 **알림을 만드는 코드가 없어 항상 빈 배열**(§4) |
| 소셜 탐색(검색 → 친구 추가) 흐름 | ✅ **충족** | 검색 정확성 수정(#166) + 받은 요청 거절 경로 신설(#174). 검색→프로필→요청→목록→수락/거절 전 구간 연결(§5) |
| 채팅 화면 디자인 FE 전달 | ❓ 회의 확인 | 코드로 판정 불가. Sprint 4 착수 조건 |

**BE로 판정 가능한 게이트 4개 중 충족 2개 · 절반 1개 · 미충족 1개.**
문서 최초 작성 시점에는 충족이 1개였으나, 같은 날 #174가 머지되며 소셜 탐색이 채워졌다(§5).
남은 차단은 **게시물 공유 API 하나**다(§3).

---

## 2. 태스크별 처리 결과

| 담당 | 태스크 | 결과 |
|---|---|---|
| 서윤님 | 알림 히스토리 저장·조회 API | 🟡 조회 API 머지(#170). 저장 트리거 미연결(§4) |
| 서윤님 | (계획 외) 받은 팔로우 요청 거절 수정 | ✅ 머지 (#174) — 게이트 차단 해소 |
| 서윤님 | Sprint 4 FCM 발송 로직 설계(선행) | ❓ 산출물 확인 안 됨 (이슈·문서 없음) |
| 건희님 | 게시물 공유 API + 통합 테스트 | ❌ PR #169 열림, 리뷰 대기·충돌·테스트 없음(§3) |
| 건희님 | 태그/메타데이터 탐색 API (JSONB) | ❌ **미착수.** 코드·이슈 모두 없음(§6) |
| 도영님 | (W7) 소셜 도메인 쿼리 최적화 | ✅ 완료 (#166, `docs/n+1-audit.md` §6) |
| 도영님 | (W8) 전체 API 명세 최신화 | ✅ 완료 (Swagger 누락 4개 + `api-spec-domains.md` 전면 갱신) |
| 도영님 | (W8) Sprint 4 아키텍처 리뷰 | ✅ 완료 (`docs/sprint4-architecture-review.md`) |
| 도영님 | (계획 외) 좋아요 데드코드 정리 + 추천 피드 노출 | ✅ 완료 (#148) |

Sprint 3 로드맵에 **기현님 태스크는 배정돼 있지 않다.** W7에 환경변수 보완(#161)을 처리했다.

### 스프린트 중 들어온 계획 외 작업

| PR | 내용 |
|---|---|
| #162 | 다른 사용자 공개 프로필 조회 API — 잔여 결함 3건(#163·#164·#165)이 여기서 나왔다 |
| #172 | JWT 인증 실패 에러 응답 처리 |

---

## 3. 게이트 차단 — PR #169 (게시물 공유 API)

8/18에 올라온 뒤 **리뷰어가 붙지 않은 채 이틀**이 지났다. 지금 상태로 머지하면 **애플리케이션이 기동하지 않는다.**

| 심각도 | 항목 | 내용 |
|---|---|---|
| 🔴 차단 | Flyway 버전 충돌 | develop에 이미 `V7__add_notification_history.sql`(#168)이 있는데 PR이 `V7__change_column_message_entity.sql`을 추가한다. 같은 버전 두 개면 기동 시 실패한다 → **V8로 변경** |
| 🔴 차단 | 적용된 마이그레이션 수정 | `V1__init.sql`을 고쳤다. 이미 V1을 적용한 DB에서는 체크섬 불일치로 마이그레이션이 멈춘다 → V1 복원 후 변경분은 새 파일로 |
| 🔴 결함 | 발신자 식별 | `UUID.fromString(userDetails.getUsername())` — `getUsername()`은 **닉네임**을 반환한다(`UserDetailsImpl`). UUID 파싱이 항상 실패한다 → 다른 컨트롤러와 같이 `getUserId()` |
| 🔴 결함 | WebSocket 인증 부재 | `/ws/**`가 permitAll이고 CONNECT 검증이 없어 `@AuthenticationPrincipal`이 `null`이다. 누구나 연결·구독할 수 있다 → `docs/sprint4-architecture-review.md` §2 |
| 🟠 결함 | 리포지토리 타입 | `ChatRoomsRepository extends CrudRepository<ChatRooms, Long>` + `Optional<Object> findById(UUID)` + 호출부 `(ChatRooms)` 캐스팅. ID 타입이 틀렸다 → `JpaRepository<ChatRooms, UUID>` |
| 🟠 게이트 | 테스트 없음 | +301/-2줄에 테스트 파일 0개. 게이트 문구가 "통합 테스트"다 |
| 🟠 | develop과 충돌 | `CONFLICTING` |
| 🟡 범위 | Sprint 4 선점 | `WebSocketConfig`·브로커 설정이 함께 들어온다. Origin이 `"*"`로 열려 있어 Sprint 1 CORS 작업과 어긋난다 |

**제안:** REST 부분과 STOMP 부분을 분리한다. 공유 메시지 생성/조회는 Sprint 3에서 닫고,
STOMP 기반은 인증 인터셉터와 함께 Sprint 4 첫 태스크로 옮긴다.
그래야 채팅 인프라를 기다리지 않고 이번 게이트를 닫을 수 있다.

---

## 4. 알림 — 조회는 되는데 데이터가 생기지 않는다

`NotificationService.save(...)`는 완성돼 있으나 **`notifications` 패키지 밖에서 호출하는 코드가 하나도 없다.**

```
[발생 지점]        [저장]                      [발송]
팔로우 요청   →   NotificationService.save()  →  FCM / Web Push
팔로우 수락        ← 호출부 0개 (구멍)            ← Sprint 4
댓글 작성
```

`GET /api/notifications`는 정상 동작하지만 언제나 `items: []`다.
게이트를 **데모로** 확인하려면 발생 지점 연결이 선행돼야 한다.

- 연결 방식(서비스 직접 호출 vs 스프링 이벤트 + `AFTER_COMMIT`)은 `docs/sprint4-architecture-review.md` §9에 정리했다.
- 팔로우가 롤백됐는데 알림만 남는 상황을 막아야 하므로 **이벤트 + 커밋 후 처리**를 권한다.
- `NotificationType.LIKE`는 폐기된 게시물 좋아요에서 온 값이라 쓰이지 않는다(#148 정리 대상).

---

## 5. 해소됨 — 받은 팔로우 요청 거절 (#163 → #174)

원인은 방향 불일치였다.

- 목록(`GET /api/follows/requests`)은 **받은** 요청, 즉 `(follower = 상대, following = 나)` 행을 반환한다.
- 거절(`DELETE /api/follows/{followingId}`)은 `(follower = 나, following = path)`로 **반대 방향**을 찾아
  행이 없으니 항상 404 `FOLLOW_001`이었다.

#174가 경로를 분리해 해결했다.

| 동작 | 경로 | 식별자 |
|---|---|---|
| 받은 요청 수락 | `PATCH /api/follows/{followId}/accept` | 팔로우 행 id |
| **받은 요청 거절** | `DELETE /api/follows/requests/{followId}` | 팔로우 행 id |
| 내 요청 취소 / 언팔로우 | `DELETE /api/follows/{followingId}` | 상대 사용자 id |

수락과 거절이 같은 식별자를 쓰게 되어 목록 응답의 `followId`를 그대로 넘기면 된다.
수신자 본인 검사(`FOLLOW_004`)와 PENDING 상태 검사도 수락과 동일하게 들어갔다.

### 남은 것

- **회귀 테스트가 없다.** 팔로우 수락·거절을 실행 경로로 검증하는 테스트가 여전히 0개다
  (`FollowCursorPaginationTest`·`FollowListQueryCountTest`는 목록만 본다).
  방향이 반대라 **항상** 실패하던 결함이었던 만큼, 같은 종류의 실수를 막는 테스트가 필요하다.
- 공개 프로필 잔여 결함 2건은 그대로다.

| 이슈 | 내용 | 게이트 영향 |
|---|---|---|
| #164 | 탈퇴 유저 프로필이 그대로 조회됨 | 흐름은 동작. 유저 검색은 #166에서 `deleted_at` 필터를 넣었는데 이 API만 빠져 있다 |
| #165 | 응답 봉투 미적용 + 이미지 키 그대로 노출 | FE 연동 품질 |

## 6. 미착수 — 태그/메타데이터 탐색 API

건희님의 Sprint 3 두 번째 태스크다. **코드도 이슈도 없다.**

선행 조건도 아직 열려 있다: `content` JSONB에서 **태그가 어느 키에 들어가는지** 합의되지 않았다
(Sprint 2 결산 §7에서 넘어온 항목). `@ValidJson`은 "유효한 JSON"인지만 보고 구조는 검증하지 않으며,
Sprint 2에서 사용되지 않던 GIN 인덱스를 제거했으므로 재도입 여부도 함께 정해야 한다.

→ Sprint 4로 이월할지, 범위에서 뺄지 **플래닝에서 결정 필요.**

---

## 7. 남은 이슈 인덱스

| 이슈 | 담당 | 상태 | 처리 시점 |
|---|---|---|---|
| #163 | 서윤님 | ✅ 닫힘 (#174) | — |
| #164 · #165 | 서윤님 | 열림 | Sprint 3 잔여 |
| #167 / PR #169 | 건희님 | 열림 | **Sprint 3 게이트 차단** — §3 리뷰 반영 후 재요청 |
| #148 | 도영님 | ✅ 닫힘 | 좋아요 코드 제거 · 추천 피드 `GET /api/posts/recommend`로 노출. `post_likes` 테이블은 유지(스키마 변경 승인 필요) |
| #140 | 서윤님 | 열림 | `findFriendFeed` 실측 — W7 최적화(#166)에서 팔로우 목록·검색을 다뤘고 친구 피드 실측은 남았다 |
| #41 | 건희님 | 열림 (7/9~) | 도메인 인터페이스 문서 정합성. 이번 명세 갱신으로 상당 부분 해소 — 재확인 후 클로즈 판단 |

---

## 8. 릴리스 상태

`develop`이 `main`보다 **15커밋** 앞서 있다 — 공개 프로필(#162), env 보완(#161), 소셜 쿼리 최적화(#166),
알림 히스토리(#170), JWT 에러 응답(#172), 명세 갱신·Sprint 4 아키텍처 리뷰(#173), 팔로우 거절 수정(#174).

Sprint 2는 마감 릴리스를 누락해 **댓글 삭제·이모지 저장이 운영에서 실패**한 적이 있다(#157).
같은 일을 반복하지 않으려면 게이트 차단 항목(§3·§5) 처리 후 `develop → main` 릴리스 PR을 올린다.

---

## 9. 결론

**Sprint 3은 게이트 4개 중 2개 충족 · 1개 절반 · 1개 미충족으로 리뷰를 마친다.**

막는 것은 이제 하나다.

- **PR #169(게시물 공유 API)** — 08-18 이후 리뷰어가 붙지 않았고, 기동 실패 사유 2건과
  런타임 결함 3건이 그대로다(§3). develop이 앞서가며 충돌 범위만 넓어졌다.

절반인 알림(§4)은 조회 API가 완성됐으므로 **발생 지점만 연결하면** 닫힌다.
이는 Sprint 4 FCM 태스크의 선행 조건이기도 하다.

완전히 손대지 못한 것은 **태그/메타데이터 탐색 API**(§6) 하나이며, Sprint 4 이월 여부를 플래닝에서 정한다.

Sprint 4는 성격이 다른 스프린트다 — 연결이 유지되는 동안 서버가 상태를 들고 있는다.
착수 전 결정 사항은 `docs/sprint4-architecture-review.md`에 정리했고, 그 체크리스트(§10)를 플래닝 안건으로 쓴다.
